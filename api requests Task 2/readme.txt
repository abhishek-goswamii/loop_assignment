run - 
    npm install
    node script.js


!!!!!important

only admin can generate api keys, so written the code, i was unable to generate api because only admins can generate it
and api cannot be used without api keys.

